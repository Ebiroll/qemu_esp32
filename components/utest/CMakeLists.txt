#idf_component_register(SRCS "qemu_test.c" 
#                       INCLUDE_DIRS ".")

set(COMPONENT_SRCS "qemu_test.c")
set(include_dirs ".")
set(COMPONENT_ADD_INCLUDEDIRS ".")

register_component()
